// psuedo for first as a user
// create a variable for title
// create a variable for body
// create a variable for save button
// add event listener to the save button
// create an instance from the inputs IF there are inputs that are strings

// querySelectors go below
var titleInput = document.querySelector('.js-title-input');
var bodyInput = document.querySelector('.js-body-input');
var saveButton = document.querySelector('.js-save-button');

// other variables go below

// eventListeners go below
saveButton.addEventListener('click', saveIdeaInput);

// functions and event handler go below
